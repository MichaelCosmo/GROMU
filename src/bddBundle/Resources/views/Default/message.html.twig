

{% for messages in message %}
    <div class="col-md-4">
        <h2>{{ messages.name }}</h2>
        {{ render(controller('bddBundle:message:comment', { 'id': messages.id })) }}
    </div>
{% endfor %}